@{
    ViewData["Title"] = "Home Page";

}

<link rel="stylesheet" href="~/css/sidebar.css" asp-append-version="true" />

@*<header>
    <nav class="d-flex flex-column">
        <div class="container-fluid">

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                <ul class="navbar-nav flex-grow-1">


                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">AluguelTech</a>
                        </li>



                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-area="" asp-controller="Funcionarios" asp-action="Index">Gerenciar Funcionários</a>
                            </li>
                        }
                    }

                    <li class="nav-item">
                        <a class="nav-link text-dark" asp-area="" asp-controller="Ferramentums" asp-action="Index">Equipamentos</a>
                    </li>



                </ul>
                <ul class="navbar-nav">



                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class="nav-item">
                            Olá, @User.Identity.Name <a class="nav-link text-dark" asp-area="" asp-controller="Funcionarios" asp-action="Logout">Sair</a>
                        </li>
                    }

                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="Funcionarios" asp-action="Login">Login</a>
                        </li>
                    }

                </ul>
            </div>
        </div>
    </nav>


</header>*@

<div class="container-fluid">
    <div class="row flex-nowrap">
        @Html.Partial("_SidebarComponent")
        <div class="col py-3 d-flex align-items-center justify-content-center">
            <div class="text-center">
                <img src="~/assets/img/alugueltechlogo.svg" />
                <p>Sua solução em aluguel de ferramentas</p>
            </div>
        </div>
    </div>
</div>


